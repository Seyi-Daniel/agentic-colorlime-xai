# Copy this file to .env and provide your own credentials locally.
# Never commit .env or paste credentials into source files.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Required only for gated ImageNet access or private Hugging Face models.
HF_TOKEN=

